{
  "title": "Climbing Stairs (Bottom-Up DP)",
  "prompt": "Solve the climbing stairs problem using bottom-up dynamic programming in `solution.py`.\n\n**Approach:**\nYou can climb 1 or 2 steps at a time. The number of distinct ways to reach step `n` follows the Fibonacci recurrence: `ways(n) = ways(n-1) + ways(n-2)`. Instead of an array, use two variables (`prev2` and `prev1`) to track the previous two values, iterating from step 3 to `n`.\n\n**Requirements:**\n1. `climb_stairs(n)` -- takes a positive integer `n` (number of steps) and returns the number of distinct ways to climb to the top\n2. Use a `for` loop iterating from 3 to n (bottom-up)\n3. Use O(1) space with two rolling variables instead of a full DP array\n4. Handle base cases: `n <= 2` returns `n`\n\n**Examples:**\n```python\nclimb_stairs(2)   # 2  (1+1 or 2)\nclimb_stairs(3)   # 3  (1+1+1, 1+2, 2+1)\nclimb_stairs(5)   # 8\nclimb_stairs(10)  # 89\n```\n\n**Complexity:** O(n) time, O(1) space.",
  "difficulty": "beginner",
  "tags": [
    "dynamic-programming",
    "fibonacci",
    "bottom-up"
  ],
  "timeEstimateSeconds": 300,
  "files": [
    {
      "path": "solution.py",
      "content": "def climb_stairs(n):\n    if n <= 2:\n        return n\n    prev2, prev1 = 1, 2\n    for _ in range(3, n + 1):\n        prev2, prev1 = prev1, prev2 + prev1\n    return prev1\n"
    }
  ],
  "hints": [
    "Base cases: there is 1 way to climb 1 step and 2 ways to climb 2 steps",
    "Each step n has ways(n) = ways(n-1) + ways(n-2), like Fibonacci",
    "Use tuple unpacking to update both variables in one line: prev2, prev1 = prev1, prev2 + prev1",
    "After the loop, prev1 holds the answer for step n"
  ],
  "assertions": {
    "perFile": {
      "solution.py": [
        {
          "type": "pythonFunctionDef",
          "name": "climb_stairs",
          "params": ["n"],
          "description": "Define a climb_stairs function that accepts an n parameter"
        },
        {
          "type": "sexpression",
          "pattern": "(for_statement)",
          "description": "Use a for loop for bottom-up iteration"
        }
      ]
    },
    "crossFile": []
  }
}
