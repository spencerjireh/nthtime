{
  "title": "Subtree of Another Tree",
  "prompt": "Write a `TreeNode` class and an `is_subtree` function in `solution.py`:\n\n**TreeNode class:**\n- `__init__(self, val=0, left=None, right=None)` -- store val, left child, and right child\n\n**Function: `is_subtree(root, sub_root) -> bool`**\nGiven the roots of two binary trees `root` and `sub_root`, return `True` if there is a node in `root` such that the subtree rooted at that node is structurally identical to `sub_root` (same structure and same values).\n\nApproach -- reuse same-tree logic:\n1. Define a helper `is_same(p, q)` that checks if two trees are identical (same as the Same Tree problem)\n2. In `is_subtree`, if `root` is `None`, return `False`\n3. If `is_same(root, sub_root)` is `True`, return `True`\n4. Otherwise, recurse: check if `sub_root` is a subtree of `root.left` or `root.right`\n\n**Complexity:** O(m * n) time where m and n are the sizes of the two trees, O(h) space.\n\n**Examples:**\n```python\n#       3           4\n#      / \\         / \\\n#     4   5       1   2\n#    / \\\n#   1   2\nroot = TreeNode(3, TreeNode(4, TreeNode(1), TreeNode(2)), TreeNode(5))\nsub = TreeNode(4, TreeNode(1), TreeNode(2))\nis_subtree(root, sub)  # True\n\n#       3           4\n#      / \\         / \\\n#     4   5       1   2\n#    / \\\n#   1   2\n#      /\n#     0\nroot2 = TreeNode(3, TreeNode(4, TreeNode(1), TreeNode(2, TreeNode(0))), TreeNode(5))\nis_subtree(root2, sub)  # False\n```",
  "difficulty": "intermediate",
  "tags": [
    "trees",
    "recursion",
    "dfs",
    "subtree",
    "intermediate"
  ],
  "timeEstimateSeconds": 360,
  "files": [
    {
      "path": "solution.py",
      "content": "class TreeNode:\n    def __init__(self, val=0, left=None, right=None):\n        self.val = val\n        self.left = left\n        self.right = right\n\n\ndef is_subtree(root, sub_root):\n    def is_same(p, q):\n        if not p and not q:\n            return True\n        if not p or not q:\n            return False\n        return p.val == q.val and is_same(p.left, q.left) and is_same(p.right, q.right)\n\n    if not root:\n        return False\n    if is_same(root, sub_root):\n        return True\n    return is_subtree(root.left, sub_root) or is_subtree(root.right, sub_root)\n"
    }
  ],
  "hints": [
    "You need a helper function to check if two trees are identical",
    "At each node in root, check if the subtree starting there matches sub_root",
    "If root is None, sub_root cannot be a subtree of it",
    "Recurse on root.left and root.right if the current node does not match"
  ],
  "assertions": {
    "perFile": {
      "solution.py": [
        {
          "type": "pythonClassDef",
          "name": "TreeNode",
          "description": "Define a TreeNode class"
        },
        {
          "type": "pythonFunctionDef",
          "name": "is_subtree",
          "params": ["root", "sub_root"],
          "description": "Define is_subtree that accepts two tree roots"
        }
      ]
    },
    "crossFile": []
  }
}
