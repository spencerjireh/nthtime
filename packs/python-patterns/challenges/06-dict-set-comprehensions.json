{
  "title": "Dict and Set Comprehensions",
  "prompt": "Write three functions in `solution.py`:\n\n**Requirements:**\n1. `word_lengths(words)` -- returns a dict mapping each word to its length using a dict comprehension\n2. `unique_chars(text)` -- returns a set of unique characters using a set comprehension (excluding spaces)\n3. `index_by(items, key)` -- takes a list of dicts and a key string, returns a dict mapping `item[key]` to `item`\n\n**Examples:**\n```python\nword_lengths([\"hello\", \"hi\"])           # {\"hello\": 5, \"hi\": 2}\nunique_chars(\"hello world\")             # {\"h\", \"e\", \"l\", \"o\", \"w\", \"r\", \"d\"}\nindex_by([{\"id\": 1, \"name\": \"A\"}], \"id\")  # {1: {\"id\": 1, \"name\": \"A\"}}\n```",
  "difficulty": "intermediate",
  "tags": [
    "comprehensions",
    "dicts",
    "sets"
  ],
  "timeEstimateSeconds": 300,
  "files": [
    {
      "path": "solution.py",
      "content": "def word_lengths(words):\n    return {word: len(word) for word in words}\n\n\ndef unique_chars(text):\n    return {c for c in text if c != \" \"}\n\n\ndef index_by(items, key):\n    return {item[key]: item for item in items}\n"
    }
  ],
  "hints": [
    "Dict comprehension: {key: value for item in iterable}",
    "Set comprehension: {expr for item in iterable}",
    "Add filter: {c for c in text if c != ' '}",
    "index_by creates a lookup table from a list of records"
  ],
  "assertions": {
    "perFile": {
      "solution.py": [
        {
          "type": "pythonFunctionDef",
          "name": "word_lengths",
          "params": ["words"],
          "description": "Define a word_lengths function that accepts a words parameter"
        },
        {
          "type": "pythonFunctionDef",
          "name": "unique_chars",
          "params": ["text"],
          "description": "Define a unique_chars function that accepts a text parameter"
        },
        {
          "type": "pythonFunctionDef",
          "name": "index_by",
          "params": ["items", "key"],
          "description": "Define an index_by function with items and key parameters"
        },
        {
          "type": "sexpression",
          "pattern": "(dictionary_comprehension)",
          "description": "Use a dict comprehension to build the result dict",
          "hint": "Dict comprehension: {key_expr: value_expr for item in iterable}"
        },
        {
          "type": "sexpression",
          "pattern": "(set_comprehension)",
          "description": "Use a set comprehension to collect unique characters",
          "hint": "Set comprehension: {expr for item in iterable}"
        }
      ]
    },
    "crossFile": []
  }
}
